export interface ProductResponse {
  id: number;
  name: string;
  price: number;
}

export interface StoreResponse {
  id: number;
  name: string;
}

export interface CustomerResponse {
  id: number;
  name: string;
}